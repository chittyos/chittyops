name: 'getChitty Creds'
description: 'Fetches ephemeral credentials from ChittyConnect via GitHub OIDC (zero secrets)'
inputs:
  api_key:
    description: 'ChittyConnect API key (deprecated — OIDC is preferred)'
    required: false
    default: ''
  purpose:
    description: 'Purpose of credential request'
    required: false
    default: 'ci-deploy'
  service:
    description: 'Service name requesting credentials'
    required: false
    default: ''
  credentials:
    description: 'Comma-separated list of credentials to fetch'
    required: false
    default: 'CLOUDFLARE_API_TOKEN,CLOUDFLARE_ACCOUNT_ID'
outputs:
  cloudflare_token:
    description: 'Ephemeral Cloudflare API token'
    value: ${{ steps.fetch.outputs.cloudflare_token }}
  account_id:
    description: 'Cloudflare Account ID'
    value: ${{ steps.fetch.outputs.account_id }}
  neon_token:
    description: 'Neon database URL'
    value: ${{ steps.fetch.outputs.neon_token }}
runs:
  using: 'composite'
  steps:
    - name: Fetch ephemeral credentials via OIDC
      id: fetch
      shell: bash
      env:
        CRED_PURPOSE: ${{ inputs.purpose }}
        CRED_SERVICE: ${{ inputs.service }}
        CRED_LIST: ${{ inputs.credentials }}
      run: |
        # Get GitHub OIDC token for ChittyConnect audience
        if [ -z "$ACTIONS_ID_TOKEN_REQUEST_URL" ]; then
          echo "::error::OIDC not available. Add 'permissions: { id-token: write }' to the calling job."
          exit 1
        fi

        OIDC_TOKEN=$(curl -sS \
          -H "Authorization: Bearer ${ACTIONS_ID_TOKEN_REQUEST_TOKEN}" \
          "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=https://connect.chitty.cc" \
          | jq -r '.value')

        if [ -z "$OIDC_TOKEN" ] || [ "$OIDC_TOKEN" = "null" ]; then
          echo "::error::Failed to obtain GitHub OIDC token"
          exit 1
        fi

        # Build credentials array from comma-separated list
        CRED_JSON=$(echo "$CRED_LIST" | jq -R 'split(",") | map(gsub("\\s";""))')

        PAYLOAD=$(jq -n --argjson creds "$CRED_JSON" '{credentials: $creds}')

        RESPONSE=$(curl -sf --max-time 30 \
          https://connect.chitty.cc/api/github-actions/credentials \
          -H "Authorization: Bearer ${OIDC_TOKEN}" \
          -H "Content-Type: application/json" \
          -d "$PAYLOAD") || {
          echo "::error::Failed to fetch credentials from ChittyConnect"
          exit 1
        }

        # Check success
        SUCCESS=$(echo "$RESPONSE" | jq -r '.success // false')
        if [ "$SUCCESS" != "true" ]; then
          ERROR_MSG=$(echo "$RESPONSE" | jq -r '.error.message // "Unknown error"')
          echo "::error::ChittyConnect error: ${ERROR_MSG}"
          exit 1
        fi

        # Extract credentials from response
        CF_TOKEN=$(echo "$RESPONSE" | jq -r '.credentials.CLOUDFLARE_API_TOKEN // empty')
        ACCT_ID=$(echo "$RESPONSE" | jq -r '.credentials.CLOUDFLARE_ACCOUNT_ID // empty')
        NEON_URL=$(echo "$RESPONSE" | jq -r '.credentials.NEON_DATABASE_URL // empty')

        # Mask all sensitive values
        for val in "$CF_TOKEN" "$NEON_URL"; do
          if [ -n "$val" ]; then
            echo "::add-mask::${val}"
          fi
        done

        # Write to outputs
        {
          echo "cloudflare_token=${CF_TOKEN}"
          echo "account_id=${ACCT_ID}"
          echo "neon_token=${NEON_URL}"
        } >> "$GITHUB_OUTPUT"
