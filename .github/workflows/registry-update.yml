name: Registry Sync

# DISABLED 2026-04-30: This workflow has been failing every 6h since 2026-02-09
# (~80 days) and even when working did nothing useful. The target endpoint
# `POST registry.chitty.cc/api/v1/sync` ignores the posted service catalog and
# just triggers an internal R2 scan — the 62-service payload from
# compliance/service-registry.yml has never been ingested.
#
# Re-enable once chittyregistry exposes a real catalog-ingestion endpoint with
# real auth. Tracking issue: CHITTYOS/chittyregistry (filed 2026-04-30).

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync-registry:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "::warning::Registry Sync is disabled — see workflow file header for context"
          exit 0
