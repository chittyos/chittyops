{
  "_note": "1Password is retired as both a credential lane and a declared authority (2026-08-21). The cold source of truth is ChittySecrets (secrets.chitty.cc), fronting the Cloudflare Secrets Store; runtime delivery is Cloudflare Secrets. The former op:// references were removed because `op` has zero accounts on the ops host and every resolution through it fails. Resolve any secret below through ch1tty -> ChittyConnect (/chico); never re-add an op:// reference or a `vault` field naming 1Password.",
  "cold_source_of_truth": "chittysecrets",
  "runtime_delivery": "cloudflare-secrets",
  "resolution_path": "ch1tty -> chittyconnect",
  "secrets": [
    {
      "name": "ORG_AUTOMATION_TOKEN",
      "source": "chittyconnect",
      "rotation_days": 30,
      "owner": "platform-security"
    },
    {
      "name": "CHITTYCONNECT_BROKER_TOKEN",
      "source": "chittyconnect",
      "rotation_days": 30,
      "owner": "platform-security"
    },
    {
      "name": "CHITTY_GATEWAY_TOKEN",
      "source": "chittyconnect",
      "rotation_days": 30,
      "owner": "platform-security"
    },
    {
      "name": "CHITTY_AGENT_TOKEN",
      "source": "chittyconnect",
      "rotation_days": 30,
      "owner": "platform-security"
    }
  ]
}
