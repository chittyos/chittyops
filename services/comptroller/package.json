{
  "name": "comptroller",
  "version": "1.0.0",
  "private": true,
  "description": "ChittyComptroller — sibling budget observer + delegated enforcer (comptroller.chitty.cc)",
  "main": "worker.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "deploy": "cf deploy"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
